# Development requirements (for Mac/non-Pi systems)
# Excludes hardware-specific packages that only work on Raspberry Pi

requests>=2.32.0
pytz>=2025.1
Pillow>=12.0.0
flask>=3.0.0
flask-cors>=6.0.2
feedparser>=6.0.11
playwright>=1.59.0
psutil>=7.0.0
git+https://github.com/hbldh/hitherdither.git

# Code quality tools
black==26.3.1
ruff>=0.14.0
pre-commit>=4.0.0

# Note: inky package excluded as it requires spidev/gpiod which are Linux-only
# The e-ink display functionality is mocked on Mac
