// Configuration and feature flags

// Favorite team configuration
const favoriteTeams = {
  mlb: 'Mariners',
  nfl: null,
  cfb: null,
};

// Feature flags
const dynamicColors = true;
const dynamicColorsOnlyFavorites = true; // Only apply dynamic colors to games with favorite teams
